###############################################
# WenXiTV / MoonTV 环境变量示例
# 复制为 .env.local 或用于部署平台的环境变量
# 按需修改后重启即可生效（部分需重新部署）
###############################################

# --- 基础账号（非 localstorage 必填） ---
# 非 localstorage 部署（redis / upstash / d1）时的站长账号与密码
USERNAME=admin
PASSWORD=admin_password

# --- 站点信息 ---
# 站点名称（影响网页标题与文案），默认 WenXiTV
NEXT_PUBLIC_SITE_NAME=WenXiTV
# 站点公告（登录后弹窗提醒），支持中文
ANNOUNCEMENT=本网站仅提供影视信息搜索服务，所有内容均来自第三方网站。本站不存储任何视频资源，不对任何内容的准确性、合法性、完整性负责。

# --- 存储类型 ---
# 播放记录与收藏的存储方式：localstorage | redis | upstash | d1
NEXT_PUBLIC_STORAGE_TYPE=localstorage

# --- Redis（原生） ---
# 当使用 NEXT_PUBLIC_STORAGE_TYPE=redis 时，设置 Redis 连接 URL
# 示例：redis://localhost:6379 或 redis://username:password@host:6379
REDIS_URL=

# --- Upstash Redis ---
# 当使用 NEXT_PUBLIC_STORAGE_TYPE=upstash 时，设置 Upstash 连接参数
UPSTASH_URL=
UPSTASH_TOKEN=

# --- Cloudflare D1 ---
# 当使用 NEXT_PUBLIC_STORAGE_TYPE=d1 时，需在部署平台绑定 D1 数据库到环境变量 DB
# 本地开发通常不支持 D1；Cloudflare Pages/Workers 通过 wrangler 绑定：vars.DB = <D1 binding>
# 这里不直接在 .env 配置 DB，以免误用；请在平台侧完成绑定。

# --- 用户注册 ---
# 是否开放注册（仅在非 localstorage 部署时生效）
NEXT_PUBLIC_ENABLE_REGISTER=false

# --- 搜索与缓存 ---
# 搜索接口最大页数（1-50），默认 5
NEXT_PUBLIC_SEARCH_MAX_PAGE=5

# --- AI4K 画质增强 ---
# 默认启用 AI4K 增强（true/false），若浏览器有本地设置则以本地为准
NEXT_PUBLIC_ENABLE_AI_4K=false
# 自动开启增强的宽度阈值（像素），例如 1280 表示播放器宽度超过 1280 时自动启用
NEXT_PUBLIC_AI_ENHANCE_WIDTH=1280
# 强制禁用 AI4K 并隐藏相关控件（true/false），优先级最高
NEXT_PUBLIC_AI_4K_FORCE_DISABLE=false

# --- 豆瓣数据源配置 ---
# 数据源请求方式：direct | cors-proxy-zwei | cmliussss-cdn-tencent | cmliussss-cdn-ali | cors-anywhere | custom
NEXT_PUBLIC_DOUBAN_PROXY_TYPE=direct
# 当选择 custom 时，配置自定义代理前缀（例如 https://proxy.example.com/fetch?url=）
NEXT_PUBLIC_DOUBAN_PROXY=

# --- 豆瓣图片代理配置 ---
# 图片代理类型：direct | server | img3 | cmliussss-cdn-tencent | cmliussss-cdn-ali | custom
NEXT_PUBLIC_DOUBAN_IMAGE_PROXY_TYPE=direct
# 当选择 custom 时，配置自定义图片代理前缀
NEXT_PUBLIC_DOUBAN_IMAGE_PROXY=

# --- 敏感词过滤 ---
# 关闭色情内容过滤（默认 false，设为 true 则不过滤）
NEXT_PUBLIC_DISABLE_YELLOW_FILTER=false

# --- 其他说明 ---
# config.json 可在仓库根目录修改，服务启动时读取；管理员配置在 /admin 页面中修改并持久化（非 localstorage 模式）。